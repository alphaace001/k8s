A Complete deployment where the mongodb is used as db.
An external service will hit the mongo express that internaly hit the mongdb pod through internal service.
Mongodb credentails are taken from secrets